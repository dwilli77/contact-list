<h1>Track 1 Burleson Families</h1>
<table>
  <thead>
    <tr>
      <th>Child</th>
      <th>Parent(s) name</th>
      <th>Last name</th>
      <th>Email</th>
      <th>Phone number</th>
      <% if current_user.is_admin %>
        <th colspan="3"  class="admin"></th>
      <% end %>
    </tr>
  </thead>

  <tbody>
    <% @families.each do |family| %>
      <tr>
        <td><%= family.kid %></td>
        <td><%= family.parent_name %></td>
        <td><%= family.last_name %></td>
        <td><%= family.email %></td>
        <td><%= family.phone_number %></td>
        <% if current_user.is_admin %>
          <td class="admin"><%= link_to 'Show', family %></td>
          <td class="admin"><%= link_to 'Edit', edit_family_path(family) %></td>
          <td class="admin"><%= link_to 'Destroy', family, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br/>

<%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'action-button logout'  %>

<br/>

<% if current_user.is_admin %>
<%= link_to 'Add New Family', new_family_path, class: "admin new-family" %>
<% end %>